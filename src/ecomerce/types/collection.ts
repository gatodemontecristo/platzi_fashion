
export interface CollectionResponseProps {
    title: string;
    price: number;
    description: string;
    id: string;
    images: string[];
}

export interface CardProps {
    title: string;
    price: number;
    description: string;
    id: string;
    image: string;
}